// Public runtime config for the static frontend (safe to expose).
// WalletConnect project id: register the dapp at https://dashboard.reown.com,
// paste the id here and redeploy. Empty id keeps MetaMask login working and
// makes the WalletConnect button explain that it is not configured yet.
window.PETIX_WALLETCONNECT_PROJECT_ID = "cb438730ff5a59a03fb1591463cfddd6";
