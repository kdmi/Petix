// Публичный адрес метаданных коллекции: /api/nft/metadata/<tokenId>
//
// Именно он уходит в контракт как baseURI и остаётся там навсегда, поэтому в
// нём не должно быть слова «demo». Логика та же, что у /api/nft-demo/metadata —
// старый путь оставлен рабочим ради уже задеплоенной демо-коллекции.
module.exports = require("../../nft-demo/metadata/[tokenId]");
